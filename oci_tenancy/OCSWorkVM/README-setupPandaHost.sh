This file will setup the storage volumes that were created on the Panda server via the Terraform script.
Make sure that the TF script in /root/ocswork/panda has successfully executed.  That is the basis of everything that
follows to setup Panda and configure the secrets.yml and hosts.yml.


