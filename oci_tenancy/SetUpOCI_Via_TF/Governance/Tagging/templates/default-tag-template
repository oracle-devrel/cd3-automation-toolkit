
resource "oci_identity_tag_default" "{{ key_tf_name }}-default" {
    #Required
    compartment_id = var.{{ default_tag_compartment }}
    tag_definition_id = oci_identity_tag.{{ key_tf_name }}.id
    value = "{{ default_tag_value }}"

    #Optional
    is_required = {{ is_required }}

}