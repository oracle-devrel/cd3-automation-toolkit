items {
        #Required
        action = "{{ action }}"
        are_invalid_characters_allowed = "{{ allow_invalid_characters }}"
        {% if http_header_size != '' %}
        http_large_header_size_in_kb = {{ http_header_size }}
        {% endif %}
     }
     ## Add_rules_here ##
