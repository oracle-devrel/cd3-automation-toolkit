items  {
        #Required
        action = "{{ action }}"

        conditions {
            #Required
            attribute_name = "{{ attribute_name }}"
            attribute_value = "{{ attribute_value }}"

            #Optional
            operator = "{{ operator }}"
        }
        redirect_uri {

            #Optional
            host = "{{ host }}"
            path = "{{ path }}"
            {% if port != '' %}
            port = "{{ port }}"
            {% endif %}
            {% if protocol != '' %}
            protocol = "{{ protocol }}"
            {% endif %}
        }
        response_code = "{{ response_code }}"
    }
    ## Add_rules_here ##
