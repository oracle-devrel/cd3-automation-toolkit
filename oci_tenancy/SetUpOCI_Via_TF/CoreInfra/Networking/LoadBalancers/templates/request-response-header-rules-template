items  {
        #Required
        action = "{{ action }}"
        header = "{{ header }}"
        {% if prefix !='' and prefix != 'nan' %}
        prefix = "{{ prefix }}"
        {% endif %}
        {% if suffix !='' and suffix != 'nan' %}
        suffix = "{{ suffix }}"
        {% endif %}
        {% if value and value != '' and value != 'nan' %}
        value = "{{ value }}"
        {% endif %}
    }
    ## Add_rules_here ##
