items  {
        #Required
        action = "{{ action }}"
        header = "{{ header }}"
        prefix = "{{ prefix }}"
        suffix = "{{ suffix }}"
        {% if value and value != '' %}
        value = "{{ value }}"
        {% endif %}
    }
    ## Add_rules_here ##
