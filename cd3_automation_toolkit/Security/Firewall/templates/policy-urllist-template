{% if count == 0 %}

url_list = {
    ##Add New urllist for {{ region|lower }} here##
}
{% else %}
{% if list_name != "" and list_name != "nan" and list_name != null %}
{{ policy_tf_name }}_{{urllist_tf_name}} = {
  urllist_name = "{{ urllist_tf_name }}"
  network_firewall_policy_id = "{{ policy_tf_name }}"
  urls = {{ url_list }}
 },

  {% endif %}
{% endif %}
